<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/stars_bg</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Space Button Style -->
    <style name="SpaceButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">16dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="elevation">8dp</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <!-- Outline Button Style -->
    <style name="OutlineButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/nebula_purple</item>
        <item name="strokeColor">@color/nebula_purple</item>
        <item name="strokeWidth">2dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Progress Dialog Theme -->
    <style name="ProgressDialogTheme" parent="Theme.Material3.Dark.Dialog">
        <item name="colorPrimary">@color/nebula_purple</item>
        <item name="android:windowBackground">@color/space_blue</item>
        <item name="android:background">@drawable/bg_gradient</item>
    </style>

    <!-- Card Style -->
    <style name="SpaceCard" parent="Widget.Material3.CardView">
        <item name="cardBackgroundColor">@color/space_blue</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">8dp</item>
        <item name="strokeColor">@color/nebula_purple</item>
        <item name="strokeWidth">1dp</item>
    </style>
</resources>
